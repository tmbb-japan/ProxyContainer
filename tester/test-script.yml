config:
  target: "http://nginx"
  phases:
    - duration: 120 # Test duration(sec)
      arrivalRate: 50 # Send 50 requests per second

scenarios:
  - flow:
      - get:
          url: "/" # Test Path
