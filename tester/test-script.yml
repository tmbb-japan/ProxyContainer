config:
  target: "http://localhost:80"
  phases:
    - duration: 60           # Test duration(sec)
      arrivalRate: 10        # Send 30 requests per second

scenarios:
  - flow:
      - get:
          url: "/"           # Test Path