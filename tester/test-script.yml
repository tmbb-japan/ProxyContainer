config:
  target: "http://nginx"
  phases:
    - duration: 1 # Test duration(sec)
      arrivalRate: 1 # Send requests per second
scenarios:
  - flow:
      - get:
          url: "/" # Test Path
